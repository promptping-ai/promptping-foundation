name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: promptping-ai/.github/.github/workflows/graphite-swift-ci.yml@main
    secrets:
      GRAPHITE_CI_TOKEN: ${{ secrets.GRAPHITE_CI_TOKEN }}
