name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    uses: promptping-ai/.github/.github/workflows/swift-release.yml@v1
    with:
      tools-macos: "pr-comments bump-version install-daemon"
      tools-linux: "pr-comments bump-version"
    secrets: inherit
